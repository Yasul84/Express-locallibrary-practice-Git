extends layout 

block content 
    h1 #{title} 

    div(style='border-style: solid; padding-top: 20px; padding-left: 20px') 
        ul 
            each genre in genre_list 
                li 
                    a(href=genre.url) | (#{genre.name})
            else 
                li There are no genres listed.
